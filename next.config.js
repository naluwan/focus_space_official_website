/** @type {import('next').NextConfig} */

const nextConfig = {
  output: 'export',
  basePath: '/focus_space_official_website',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
